# IAM-004 synthetic verification evidence

This directory records source-only, synthetic IAM-004 verification. No production factor, key, session, account, or protected surface was activated.

Local evidence is generated by `pnpm iam004:verify`, `pnpm db:verify`, the full repository checks, and the five required GitHub gates. The real integration suite starts isolated PostgreSQL and Redis containers and uses SEC-003's test-only provider. It verifies encrypted storage, replay and recovery-code one-winner behavior, factor lifecycle, session rotation, and IAM-003 recovery preservation. CI run IDs and any unresolved review findings belong in the draft PR, not in fabricated static evidence.

Independent Identity/Security, Security/Cryptography, Data/Database, API/Platform, QA/Security, and applicable DevOps/Security review remains required before merge. Production activation has the separate blockers in [MFA.md](../../../MFA.md).

