{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@noma/web...",
  "outputDirectory": "apps/web/.next",
  "git": {
    "deploymentEnabled": {
      "main": false
    }
  }
}
